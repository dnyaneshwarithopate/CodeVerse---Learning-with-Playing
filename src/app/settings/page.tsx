import { AppLayout } from '@/components/app-layout';

export default function SettingsPage() {
  return (
    <AppLayout>
      <div className="flex items-center justify-center h-full">
        <h1 className="text-4xl font-bold">Settings Page (Placeholder)</h1>
      </div>
    </AppLayout>
  );
}
