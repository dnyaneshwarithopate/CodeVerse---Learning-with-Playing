import { AppLayout } from '@/components/app-layout';

export default function LeaderboardPage() {
  return (
    <AppLayout>
      <div className="flex items-center justify-center h-full">
        <h1 className="text-4xl font-bold">Leaderboard Page (Placeholder)</h1>
      </div>
    </AppLayout>
  );
}
