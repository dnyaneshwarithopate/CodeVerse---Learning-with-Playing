import { AppLayout } from '@/components/app-layout';

export default function ProfilePage() {
  return (
    <AppLayout>
      <div className="flex items-center justify-center h-full">
        <h1 className="text-4xl font-bold">Profile & Progress Page (Placeholder)</h1>
      </div>
    </AppLayout>
  );
}
