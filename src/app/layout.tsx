// This file was intentionally left blank. 
// The admin layout is handled by the src/components/admin-layout.tsx component,
// which is directly imported into each admin page.
// This approach was taken to simplify the file structure for this project.
export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>
}
